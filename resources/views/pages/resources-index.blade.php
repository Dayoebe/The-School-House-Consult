<x-page-hero eyebrow="Resources & Insights" title="Ideas to inform. Perspectives to explore." text="A growing collection of articles and resources on education, leadership and professional practice." /><section class="section"><div class="container"><div class="section-top"><x-section-heading eyebrow="From The School House Consult" title="Latest resources" />@if($categories->isNotEmpty())<div class="category-filter"><label for="category">Filter by category</label><select id="category" wire:model.live="category"><option value="">All categories</option>@foreach($categories as $item)<option value="{{ $item->slug }}">{{ $item->name }}</option>@endforeach</select></div>@endif</div><div wire:loading role="status">Updating resources…</div><div wire:loading.class="is-loading">@if($articles->isEmpty())<x-empty-state title="More to explore, soon" :text="$category ? 'No published articles match this category. Try another category.' : 'Articles and resources will appear here when they are published. Explore our services or get in touch to discuss an educational topic.'" />@else<div class="grid-three">@foreach($articles as $article)<x-article-card :article="$article" />@endforeach</div><div class="pagination">{{ $articles->links() }}</div>@endif</div></div></section><x-newsletter /><x-cta />